
- Would be nice if we could specify version number in one place and
  have rockspec and C source auto-updated upon build

- Blink lib at https://github.com/todbot/blink1.git
  can we detect if it exists and, if not, download and build it?


